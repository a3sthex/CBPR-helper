# Ревизия репозитория и план улучшений

**Дата:** 2026-08-28
**Ветка:** `arena/01a045ac-cbpr-helper`
**Статус:** отчёт по аудиту + приоритизированный план. Спорные/деструктивные пункты вынесены в «Открытые вопросы».

## 1. Снимок состояния

| Метрика | Значение |
|---|---|
| Python backend | `app/server.py` — ~20 200 строк, 191 HTTP-маршрут |
| Frontend | vanilla JS без сборки: `app.js` 4 389 + `ncnet.js` 584 + `creation-data.js` 762 + сопутствующие |
| Тесты | 6 файлов, 260 тестов — **все зелёные** (unittest, ~20 c) |
| CI | не было → подготовлен рецепт `docs/ci-tests-workflow.yml`; **заблокировано правами токена** (нет scope `workflows` — GitHub не даёт пушить файлы в `.github/`). Добавить вручную: скопировать файл в `.github/workflows/tests.yml` через веб-интерфейс GitHub одним коммитом. |
| Каталог | `app/data/items.json` — 1092 предмета из `Data Pool.xlsx` |
| Книги | 11 PDF как LFS-указатели; полные тексты извлечены в `extracted/`, покатегорный индекс в `extracted/analysis/` |
| `.git` (до чистки истории) | ~287 МБ — из-за частей книг в истории ветки |

Проверенный скан рисков:

- SQL через f-строки — 6 мест, все с внутренними идентификаторами (PRAGMA/ALTER/UPDATE по белым спискам колонок). Инъекций не найдено.
- `eval/exec/os.system/subprocess` — нет.
- TODO/FIXME в коде — 1 шт.
- Секреты — только через env, в git нет.

## 2. Выполнено этой ревизией

1. `uploads/` очищен от частей книг (было ~186 МБ в дереве), остался README-инструктаж.
2. `.gitignore`: `books/`, `extracted/images/` — тяжёлые артефакты не в git.
3. `.gitattributes`: `uploads/` исключена из LFS (иначе веб-загрузка PDF превращалась в указатели).
4. CI: подготовлен workflow (unittest-стек + `node --check` на JS) — лежит рецептом в `docs/ci-tests-workflow.yml`, в `.github/` его нужно положить вручную (см. таблицу выше).
5. README: раздел «Книги-источники и извлечение текста».

## 3. Открытые вопросы чистки (нужно решение владельца)

1. **История git.** ~~Части книг всё ещё внутри истории~~ → **решено (28.08):** история ветки переписана без `uploads/*.pdf` (git filter-repo), force-push выполнен, `.git` сжался с ~287 МБ до ~8 МБ. HEAD-дерево побайтово совпало (`c77f1411…`) — содержимое не пострадало. Книги доступны у владельца на Яндекс.Диске.
2. **Корень репозитория.** ~~11 LFS-PDF + источники в корне~~ → **решено (28.08):** PDF переехали в `library/`, `Data Pool.xlsx` и `Spes Desperata.docx` — в `import/`, путь в `app/import_data.py` обновлён, тесты зелёные.

## 4. План улучшений (приоритеты)

### P0 — гигиена процесса (дешёво, сразу)

- [x] CI на push/PR (unittest + node --check).
- [ ] В README уже есть команды проверки — держать их синхронными с CI.
- [ ] Status badge в README после первого прогона CI.

### P1 — структура backend (самая большая боль)

`server.py` — 20k строк монолитом, отработает любая правка долго из-за прокрутки. Размеченные секции уже есть (`# ---- каталог`, `# ---- правила`, `# ---- Session Recap`…), тесты грузят сервер через `importlib` — можно дробить безопасно:

1. ~~Пилот: выделить `app/media.py`~~ → **сделано (28.08):** вынесены `app/core.py` (пути, ApiError, роли, разрешения, visibility-дэфолты) и `app/media.py` (валидация/хранение изображений + миксин MediaHandlers). Паттерн доказан: 260/260 тестов зелёные без изменения логики.

2. ~~Каталог/эффекты~~ → **сделано (28.08):** `app/catalog.py` (681 строка: загрузчики items.json/effects.json, валидация эффектов, пэйлоады предметов, профили кибердеков/дальностей).\n   **Правила, вынесенные по итогам двух итераций:**
   - перед срезом обязателен AST-аудит внешних имён блока (regex-список кандидатов пропустил `SKILL_BY_NAME`/`math` — тесты поймали, но дешевле ловить до коммита);
   - литеральные константы → в `core.py`; вычисляемые/кросс-доменные — временный `catalog.bind(...)` (позднее связывание), пока их домены не выедут;
   - движок модификаций (weapon/vehicle eval) НЕ вырезан до выноса inventory/tech-maker хелперов — единственная пока точка зацепки блоков между собой.

3. ~~Правила~~ → **сделано (28.08):** `app/rules.py` (686 строк: таблицы STAT/SKILL/ROLE, DV и криты RU/EN, пайплайн модификаторов, рантайм эффектов, `derive()`). Три кросс-доменных хелпера подключены через `rules.bind(...)`; `rules` напрямую импортирует три функции из `catalog` (цикла нет: `catalog` на уровне модуля зависит только от `core`).
   **Доп. урок:** AST-аудит внешних имён должен учитывать имена, приходящие через re-export (`from catalog import ...` в server.py) — `visit_Name`/`Assign` такие не видят, поэтому `validate_effect_definition` выловил только прогон тестов.

4. ~~Mod-engine~~ → **сделано (28.08):** `app/mod_engine.py` (854 строки: схемы модификаций оружия/транспорта/кибердеков, общий боезапас, эффективные характеристики). `INSTANCE_ID_RE` уехал в core. Три хелпера ждут домены inventory/tech-maker — через `mod_engine.bind(...)`.
   **Ещё урок:** вычисляемые константы (`re.compile(...)`) тянут за собой импорт stdlib в принимающем модуле — переносил `INSTANCE_ID_RE` в core без `import re`, поймал на этапе smoke-теста импорта. Теперь чек-лист: py_compile + import-модуль + полный прогон перед коммитом.

5. ~~Валидация персонажа + Tech Maker~~ → **сделано (28.08):** `app/charbuild.py` (2098 строк, 67 публичных имён).

5.4.0. ~~Handler: personas/contracts/jobs~~ → **сделано (28.08):** `app/personas_api.py` (`PersonasMixin`, 25 методов: персоны, контракты с афтермат-ипром, джобы фиксера). Драйвер научился захватывать декораторы (`@atomic_endpoint`) в сегмент.

4.1. ~~Handler: feed/news/storyline~~ → **сделано (28.08):** `app/feed_api.py` (`FeedMixin`, 21 метод: лента/комменты/модерация, новости, сюжетки). Драйвер потоковый, smoke-киллы — только по явному PID-файлу.

4.2. ~~Handler: account-кластер~~ → **сделано (28.08):** `app/account_api.py` — первый миксин-раздел `Handler` (15 методов, `AccountMixin`). Механика драйвера: AST-сегменты методов → модуль-миксин → базы класса наследованием; импорты по аудиту. Дальше кластерами: feed/news, personas/contracts, sessions, character, admin…

4.3. ~~HTTP-инфра~~ → **сделано (28.08):** `app/httpkit.py` (таблица локализации ошибок, theme-валидация, `attach_network_media`, `atomic_endpoint`, `q1`, `rx`). Факт поля: `Handler(MediaHandlers, ...)` — миксин-паттерн уже существует (media.py); финальный раздел Handler пойдёт миксинами по кластерам методов. Пуши explicit-refspec не двигают origin-tracking — сверка только через `git ls-remote`.

4.4. ~~Auth-слой~~ → **сделано (28.08):** `app/auth.py` (9 имён, `PBKDF_ITERS` в core). Всё до-баннерное (бд…auth) извлечено; в server.py остаётся только http-хвост.

4.5. ~~Пять малых доменов~~ → **сделано (28.08):** `recap.py` (рекапы/хроника/persona-bootstrap), `campaign.py` (часы+даунтайм слиты), `locations.py`, `memorial.py`, `crew.py` (включая `crew_reputation_map` — рынок перепривязан).
   **Урок:** реальный двусторонний кросс-импорт night_market↔crew отброшен — bind остаётся инструментом; порядок среза зон — строго вперёд (конец = следующий баннер), обратный ход ломает маркеры.

4.6. ~~ensure_progression~~ → **сделано (28.08):** переехал в `charbuild.py` (нормализаторы листа персонажа рядом с создателями). Вызовов `.bind()` стало меньше на 2 (db/crew без bind вообще); остаточные bind — только анти-циклические по дизайну.

5. ~~Ночной рынок~~ → **сделано (28.08):** `app/night_market.py` (ротации по сиду дня, постоянный ассортимент PERMANENT_SUPPLY переехал вместе с доменом, стоки, цены). `MOSCOW` — в core. `crew_reputation_map` ждёт домена crew — через `night_market.bind()`.
   **Урок:** детектор stdlib-импортов допустил слепую зону (`datetime`/`timedelta`/`sqlite3` не вошли в список кандидатов); расширил до полного списка модулей + re-scan имён после среза обязателен.

7. ~~Слой данных~~ → **сделано (28.08):** `app/db.py` (SCHEMA, миграции, bootstrap ensure_*, rate-limit, бэкап-обвязка; 118 имён). `DB_PATH`/`BACKUP_DIR` уехали в core как конфиг. Тест-монопатчи ретаргетированы на модуль-владелец (`server._db_mod`) — правило на будущие итерации: при переносе функции все monkeypatch-контракты тестов смотреть по `grep 'server\.<NAME>' tests/`.

8. ~~Inventory item-instances~~ → **сделано (28.08):** `app/inventory.py` (380 строк: instance-id, legacy-разрешение каталога, стекуемость, persist/read модификаций, слоты оружия). Заодно убран лишний дубль `INSTANCE_ID_RE` в server.py. Поздние привязки (`bind()`) на трёх модулях продолжают работать через реэкспорт в server.py без изменения текста вызовов.
2. Паттерн повторить по доменам: `auth`, `personas`, `contracts`, `feed`, `sessions+net`, `nightmarket`, `characters+sheet`, `admin`, `catalog`, `rules` (правила уже почти чистая секция 1610–2296).
3. Роутинг оставить в одном `ROUTES` — он хорошо читается таблицей.
4. Каждый шаг = отдельный коммит + прогон 260 тестов.

Риск: merge-конфликты с параллельными правками. Сейчас на ветке никого, кроме этой сессии, нет — окно удобное.

### P1 — структура frontend

`app.js` 4,4k строк: выделить view-слои (dossier, market, feed, contracts) в модули вида `app/static/views/*.js`, `index.html` подключает по очереди — без сборки, в духе проекта. Начать с самого автономного (market).

### P2 — данные и справочник

- `import_data.py` (638 строк) — добавить режим `--check`: валидация `Data Pool.xlsx` без перезаписи `items.json` (ловить битые строки до импорта).
- Сверить каталог с извлечёнными предметными страницами (`extracted/analysis/`, категория `items_gear`, 548 стр.): найти предметы из книг, которых нет в справочнике, и наоборот — записи без источника.
- `docs/ncnet-sources.md`: проставить ссылки на диапазоны страниц из `categories.json` (сейчас вручную: "Corebook p. 129").

### P2 — эксплуатация

- Логи: сейчас простые print — ввести `logging` с уровнями + request id в access-логе (nginx корреляция).
- Метрики: endpoint `/api/stats` есть; добавить размеры таблиц и возраст последнего бэкапа.
- Бэкапы: есть создание/verify/download — добавить автопрогон verify по таймеру и алерт при неудаче (в VK outbox, если настроен).

### P3 — дальше

- OpenAPI-таблица из `ROUTES` в `docs/api.md` (генерация скриптом, не руками).
- `i18n.js` audit: свести захардкоженные RU/EN строки из `app.js` в словари.
- Pre-push hook: `python3 -m unittest discover -s tests -q` (сейчас 20 c — терпимо локально).

## 5. Что НЕ трогаем

- Стек без зависимостей (stdlib + vanilla JS) — осознанный выбор проекта, менять не предлагается.
- SQLite — для нагрузки кампании достаточно; WAL и `BEGIN IMMEDIATE` уже стоят.
